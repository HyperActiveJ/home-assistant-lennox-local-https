{
  "config": {
    "title": "Lennox HTTPS",
    "step": {
      "user": {
        "title": "Define your Lennox HTTPS connection",
        "data": {
          "host": "The ip-address of your Lennox HTTPS",
          "name": "The prefix to be used for your Lennox HTTPS sensors",
          "port": "The TCP port on which to connect to the Lennox HTTPS server"
        }
      }
    },
    "error": {
      "already_configured": "Device is already configured"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}
